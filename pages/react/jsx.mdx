import { CodeAnn } from '@components/code-ann'
import { Box, Boxes } from '@components/box'
import { InlineMath } from 'react-katex'
import { Figure } from '@components/figure'

<style jsx>{`
  h2 {
    padding-bottom: 0.5rem;
    border-bottom: 2px solid #bbb;
  }
`}</style>`

# 🚧 Справка по JSX

JSX — это язык, на котором мы пишем страницы в React. Он позволяет смешивать HTML и JavaScript.

<div id="elements">
## `<...>` — создание элементов в JSX
</div>

<Boxes>
  <Box>
    JSX-файлы выглядят как обычный JavaScript, но в них можно использовать `<...>` для создания элементов.
  </Box>
  <Box>
    <CodeAnn ok="На странице будет один заголовок">
      ```jsx
      export default function Home() {
        // mark
        return <h1>Привет!</h1>;
      }
      ```
    </CodeAnn>
  </Box>
</Boxes>

<Boxes>
  <Box>
    С элементами можно обращаться так же, как и с любыми другими значениями. Их можно сохранять в переменные, передавать в функции, возвращать из функций и т.д.
  </Box>
  <Box>
    <CodeAnn ok="На странице будет три одинаковых заголовка">
      ```jsx
      export default function Home() {
        // mark
        const title = <h1>Привет!</h1>;
        return (
          <>
            {title}
            {title}
            {title}
          </>
        );
      }
      ```
    </CodeAnn>
  </Box>
</Boxes>

<Boxes id="jsx-fragment-has-no-corresponding-closing-tag">
  <Box>
    В отличие от HTML, в JSX элементы обязательно должны быть закрыты. Например:
    
    - нельзя написать `<br>` или `<img ... >`,
    - нужно писать `<br />` или `<img ... />`.
  </Box>
  <Box>
    <CodeAnn error="JSX fragment has no corresponding closing tag">
      ```jsx
      export default function Home() {
        return (
          <>
            <h1>Привет!</h1>
            // mark
            <img src="/example.png">
          </>
        );
      }
      ```
    </CodeAnn>
  </Box>
</Boxes>

<Boxes id="self-closing-tags">
  <Box>
    Если у элемента нет содержимого, то его можно закрыть с помощью `/>`:
  </Box>
  <Box>
    <CodeAnn ok="Теперь хорошо">
      ```jsx
      export default function Home() {
        return (
          <>
            <h1>Привет!</h1>
            // mark[31:32]
            <img src="/example.png" />
          </>
        );
      }
      ```
    </CodeAnn>
  </Box>
</Boxes>

<div id="fragments">
## `<> ... </>` — фрагменты
</div>

<Boxes id="jsx-expressions-must-have-one-parent-element">
  <Box>
    Нельзя вернуть несколько элементов сразу или записать их в переменную:
  </Box>
  <Box>
    <CodeAnn error="JSX expressions must have one parent element">
      ```jsx
      export default function Home() {
        return (
          // mark
          <h1>Привет!</h1>
          // mark
          <p>Как дела?</p>
        );
      }
      ```
    </CodeAnn>
  </Box>
</Boxes>

<Boxes>
  <Box>
    Чтобы так сделать, надо обернуть их в пустой элемент — фрагмент.
    
    В чистом HTML такого не существует, так что в итоге он исчезнет.
  </Box>
  <Box>
    <CodeAnn ok="Работает">
      ```jsx
      export default function Home() {
        return (
          // mark
          <>
            <h1>Привет!</h1>
            <p>Как дела?</p>
          // mark
          </>
        );
      }
      ```
    </CodeAnn>
  </Box>
</Boxes>