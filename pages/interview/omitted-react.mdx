# Что я не рассказал про React (но что могут спросить на интервью)

## Компоненты с помощью `class`

В этом курсе я упоминал только функциональные компоненты. Бывают еще [компоненты-классы](https://reactjs.org/docs/react-component.html), там свои заморочки, `componentDidMount` и так далее. Они считаются устаревшими, поэтому я их не рассказываю вообще, но на интервью могут спросить, потому что на интервью вообще что угодно могут спросить.

